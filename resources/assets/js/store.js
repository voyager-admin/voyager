import { reactive } from 'vue';

export default reactive({
    sidebarOpen: true,
    darkmode: 'system',
    systemDarkmode: false,

    formfields: [],
    breads: [],

    pageLoading: true,
    sidebarOpen: true,
});